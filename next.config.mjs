// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: '/TKBOnlineShowroom',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
// module.exports = nextConfig;